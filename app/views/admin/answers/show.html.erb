<div>
  <h2 class="mt-2 mb-2"><%= t('.header') %>: "<%= @answer.question.body %>"</h2>

  <p>
    <b><%= t('.answer') %>:</b>
    <%= @answer.body %>
  </p>

  <p>
    <b><%= t('.correct') %>:</b>
    <%= @answer.correct %>
  </p>

  <%= link_to t('common.back'), admin_test_question_path(current_test, current_question), class: "btn btn-outline-primary mb-2" %>
  <%= link_to t('common.edit'), edit_admin_test_question_answer_path(current_test, current_question, @answer), class: "btn btn-outline-primary mb-2" %>
</div>
