<p>Test title: <b><%= render plain: @test.title %></b></p>
<p>Level: <b><%= render plain: @test.questions.count %></b></p>

<p><%= link_to 'Create new question', new_admin_test_question_path(@test) %></p>

<table>
  <tr>
    <th>Question</th>
    <th colspan='3'>Actions</th>
  </tr>

  <%= render @test.questions, current_test: @test %>
</table>

<%= link_to 'Back', admin_tests_path %>
