<p><%= t('.test_title') %>: <b><%= render plain: @test.title %></b></p>
<p><%= t('common.level') %>: <b><%= render plain: @test.questions.count %></b></p>

<p><%= link_to t('.add_question'), new_admin_test_question_path(@test) %></p>

<table>
  <tr>
    <th><%= t('.questions') %></th>
    <th colspan='3'><%= t('common.actions') %></th>
  </tr>

  <%= render @test.questions, current_test: @test %>
</table>

<%= link_to t('common.back'), admin_tests_path %>
